%% Get files and move them to another location 
list = dir();
list([list.isdir]) = [];
list={list.name};
%% 
Folder=[{'all_ripples'} {'500'} {'1000'}];



 mkdir(Folder{FiveHun+1})
for nmd=1:length(list)
movefile (list{nmd}, 'probando')
end
%%
out = cellfun(@(x)regexp(x,'probandfo'),{va.name},'un',0);